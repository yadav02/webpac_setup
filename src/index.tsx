import ReactDOM from 'react-dom';
import { App } from "./App";

// const root : HTMLElement | null = ReactDOM.createRoot(document.getElementById("root"));

ReactDOM.render(<App/>, document.getElementById('root'))
// root.render(<App />);